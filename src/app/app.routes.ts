// src/app/app.routes.ts
import { Routes } from '@angular/router';

export const routes: Routes = [
    // Add routes here if needed in the future
    // For now, we only have the main chat interface
];